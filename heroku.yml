
setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python ar/manage.py migrate
    - python ar/manage.py collectstatic --noinput
run:
  web: gunicorn config.wsgi

